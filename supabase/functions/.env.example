# Cópia para `supabase/functions/.env` ao correr `supabase functions serve` localmente.
# Não commitar `.env` com chaves reais. Em produção: Dashboard → Edge Functions → Secrets.
MISTRAL_API_KEY=
# Opcional (predefinição: mistral-small-latest)
MISTRAL_MODEL=mistral-small-latest
